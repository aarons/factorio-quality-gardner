**General Rules**

- Avoid using prefixes and abbreviations (ie, "gg-template") as they are unclear and don't stand on their own
- Spell out identifiers (`position_key`, `available_by_tier`, not `pos_key`/`avail`).



&nbsp;